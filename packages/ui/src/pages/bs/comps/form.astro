---
import Layout from "@/layouts/LayoutBS.astro";
import PageTitle from "@/comps-bs/PageTitle.astro";
import FormControl from "@/comps-bs/FormControl.astro";

const page = {
  title: "Form",
  breadcrumb: [{ label: "Home", path: "/bs" }, { label: "Form" }],
};
---

<Layout>
  <section>
    <PageTitle page={page} />
    <form class="needs-validation was-validated-">
      <FormControl
        type="input"
        subtype="email"
        id="email"
        label="Email"
        help="Enter your valid email address"
        placeholder="Enter your valid email address"
      />

      <FormControl type="input" subtype="text" id="username" label="Username" />

      <FormControl type="input" subtype="number" id="age" label="Age" />

      <FormControl
        type="input"
        subtype="password"
        id="password"
        label="Password"
      />

      <FormControl type="select" id="role" label="Role" options="" />

      <button type="submit" class="btn btn-primary">Submit</button>
    </form>
  </section>
</Layout>
