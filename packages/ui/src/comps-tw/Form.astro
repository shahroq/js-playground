---
const { props } = Astro;
---

<form class="form valid- invalid-">
  {
    props.legend ? (
      <fieldset>
        <legend>{props.legend}</legend>
        <slot />
      </fieldset>
    ) : (
      <slot />
    )
  }
</form>
