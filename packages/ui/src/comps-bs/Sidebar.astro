---
import site from "@gpublic/json/site.json";
import Nav from "./Nav.astro";
import type { NavItem } from "@gpublic/types/types";
import { filterNavItems, prefixNavPaths } from "@gpublic/utils/nav";
import NavUser from "@/comps-bs/NavUser.astro";
import NavSidebar from "./NavSidebar.astro";

let navItems: NavItem[] = site.navSide;
navItems = filterNavItems(navItems, "ui");
navItems = prefixNavPaths(navItems, "/bs");

const curPath = Astro.url.pathname;
---

<div class="offcanvas-md offcanvas-end" tabindex="-1" id="sidebar-menu">
  <div class="offcanvas-header">
    <h4 class="offcanvas-title">Pixel</h4>
    <button
      type="button"
      class="btn-close"
      data-bs-dismiss="offcanvas"
      data-bs-target="#sidebar-menu"></button>
  </div>
  <div class="offcanvas-body d-flex flex-column pt-lg-3">
    <Nav
      navItems={navItems}
      curPath={curPath}
      className={"nav flex-column mb-auto"}
    />
    <hr />
    <NavUser />
  </div>
</div>
