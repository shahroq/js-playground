// jsonplaceholder
/*
export type JPResponse = {
  albumId: number;
  title: string;
  url: string;
};
*/

// cat api
export type CatApiResponse = {
  id: string;
  height: number;
  width: number;
  url: string;
};
